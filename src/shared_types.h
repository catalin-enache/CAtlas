#ifndef ATLAS_SHARED_TYPES_H
#define ATLAS_SHARED_TYPES_H

typedef struct {
    char * name;
    int width;
    int height;
    float shrink;
    int offset_x;
    int offset_y;
} AbsolutePositioningEntry;

#endif //ATLAS_SHARED_TYPES_H